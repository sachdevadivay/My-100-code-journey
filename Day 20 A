#include <stdio.h>

int main() {
    int num, digit, product = 1, hasOdd = 0;

    
    printf("Enter a number: ");
    scanf("%d", &num);

    while (num > 0) {
        digit = num % 10;       
        if (digit % 2 != 0) {   
            product *= digit;
            hasOdd = 1;         
        }
        num = num / 10;         
    }

    if (hasOdd)
        printf("%d\n", product);
    else
        printf("1\n");   

    return 0;
}
